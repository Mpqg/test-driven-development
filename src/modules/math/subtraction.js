/**
 * File: substraction.js
 * 
 * For the case of function subtract
 * Example: subtract(1, 2) // -1
 * Example: subtract(0.1, 0.2) // -0.1
 * 
 * For the case of function subtractArray
 * Example: subtractArray(1, 2, 3) // -4
 * Example: subtractArray(0.1, 0.2, 0.3) // -0.4
 * 
 * For the case of function absolute
 * Example: absolute(1) // 1
 * Example: absolute(-1) // 1
 * 
 * For the case of function absoluteArray
 * Example: absoluteArray(1, 2, 3) // 6
 * Example: absoluteArray(-1, -2, -3) // 6
 * 
 */

export function subtract () {
  return 0;
}

export function subtractArray () {
  return 0;
}

export function absolute () {
  return 0;
}

export function absoluteArray () {
  return 0;
}
