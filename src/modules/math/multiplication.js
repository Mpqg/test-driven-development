/**
 * File: multiplication.js
 * 
 * For the case of function multiply
 * Example: multiply(1, 2) // 2
 * Example: multiply(0.1, 0.2) // 0.02
 * 
 * For the case of function multiplyArray
 * Example: multiplyArray(1, 2, 3) // 6
 * Example: multiplyArray(0.1, 0.2, 0.3) // 0.006
 * 
 * For the case of function factorial
 * Example: factorial(1) // 1
 * Example: factorial(2) // 2
 * Example: factorial(3) // 6
 * Example: factorial(4) // 24
 * 
 */

export function multiply () {
  return 0;
}

export function multiplyArray () {
  return 0;
}
