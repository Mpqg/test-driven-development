/**
 * File: logical.js
 * 
 * For the case of function isEven
 * Example: isEven(2) // true
 * Example: isEven(3) // false
 * 
 * For the case of function isOdd
 * Example: isOdd(2) // false
 * Example: isOdd(3) // true
 * 
 * For the case of function isPrime
 * Example: isPrime(2) // true
 * Example: isPrime(3) // true
 * Example: isPrime(4) // false
 * 
 * For the case of function isInteger
 * Example: isInteger(2) // true
 * Example: isInteger(2.1) // false
 * 
 * For the case of function isPositive
 * Example: isPositive(2) // true
 * Example: isPositive(-2) // false
 * 
 * For the case of function isNegative
 * Example: isNegative(2) // false
 * Example: isNegative(-2) // true
 * 
 */

export function isEven () {
  return false;
}

export function isOdd () {
  return false;
}

export function isPrime () {
  return false;
}

export function isInteger () {
  return false;
}

export function isPositive () {
  return false;
}

export function isNegative () {
  return false;
}
